@* @model CourseIndexViewModel *@

<!-- Courses Section Start -->
@* <div class="courses-section">
    <div class="courses">
        <div class="courses-heading">
            <div class="courses-title">Courses</div>
            <div class="courses-inputs">
                <form class="courses-inputs">
                    <div class="select">
                        <div class="selected">All categories</div>
                        <div class="select-options">
                            <div class="option" data-value="all">All categories</div>
                            @if (Model.Categories != null)
                            {
                                foreach (var category in Model.Categories)
                                {
                                    <div class="option" data-value="@category.CategoryName">@category.CategoryName</div>
                                }
                            }
                        </div>
                    </div>

                    <div class="input-group">
                        <input id="searchQuery" name="searchQuery" placeholder="Search courses">
                        <i class="fas fa-search"></i>
                    </div>
                </form>
            </div>
        </div>
        <div class="course-items">
            @if (TempData.ContainsKey("CourseSavedMessage"))
            {
                <script>
                    alert("@TempData["CourseSavedMessage"]");
                </script>
            }

            @if (Model.Courses != null)
            {
                <div class="items">
                    @foreach (var course in Model.Courses)
                    {
                        <div class="course-item">
                            <a asp-action="Details" asp-controller="Courses" asp-route-id="@course.Id" class="c-item">
                                <div class="couese-card">
                                    <div class="card-img">
                                        <img class="course-img" src="@course.Image" alt="@course.Title">
                                    </div>
                                    <div class="course-content">
                                        <h3 class="course-title">@course.Title</h3>
                                        <p class="course-author">By @course.Author</p>
                                        <div class="course-prices">
                                            <div class="course-price">@course.Price</div>
                                            @if (!string.IsNullOrEmpty(course.DiscountPrice))
                                            {
                                                <div class="course-discount">@course.DiscountPrice</div>
                                            }
                                        </div>
                                        <div class="course-time">
                                            <div class="course-time-icon">
                                                <i class="fa-regular fa-clock"></i> @course.Hours hours
                                            </div>
                                            <div class="course-likes">
                                                <i class="fa-regular fa-thumbs-up"></i> @course.LikesProcent (@course.LikesInNumber)
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </a>
                            <form method="post" asp-action="SaveCourse" asp-controller="Courses" asp-route-courseId="@course.Id">
                                <button type="submit" class="course-icon">
                                    <i class="fa-regular fa-bookmark"></i>
                                </button>
                            </form>
                            @if (course.IsBestSeller)
                            {
                                <div class="best-seller">Best Seller</div>
                            }
                        </div>
                    }
                </div>

            }
        </div>
        @if (Model.Courses != null)
        {
            @if (Model.Pagination != null && Model.Pagination.TotalPages > 1)
            {
                <div class="courses-pagenation">
                    <div class="slider-container">
                        <div class="slider-arrow">
                            @if (Model.Pagination.CurrentPage > 1)
                            {
                                <a class="slide" href="@Url.Action("Index", new { pageNumber = Model.Pagination.CurrentPage - 1, pageSize = Model.Pagination.PageSize})"><i class="fas fa-arrow-left" id="prev-arrow"></i></a>
                            }
                        </div>
                        <div class="slider">
                            @for (int i = 1; i <= Model.Pagination.TotalPages; i++)
                            {
                                if (i == Model.Pagination.CurrentPage)
                                {
                                    <div class="slide active">@i</div>
                                }
                                else
                                {
                                    <a class="slide" href="@Url.Action("Index", new { pageNumber = i, pageSize = Model.Pagination.PageSize})">@i</a>
                                }
                            }

                        </div>
                        <div class="slider-arrow">
                            @if (Model.Pagination.CurrentPage < Model.Pagination.TotalPages)
                            {
                                <a class="slide" href="@Url.Action("Index", new { pageNumber =  Model.Pagination.CurrentPage + 1, pageSize = Model.Pagination.PageSize})"><i class="fas fa-arrow-right" id="next-arrow"></i></a>
                            }
                        </div>
                    </div>
                </div>
            }

        }
    </div>
</div> *@
<!-- Courses Section End -->
